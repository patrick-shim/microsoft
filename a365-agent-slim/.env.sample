# ═══════════════════════════════════════════════════════════════════════════════
#  a365-agent-slim — environment template
#  Copy this file to `.env` and fill it in.  `.env` is gitignored — NEVER commit it.
#
#  This is the LOCAL demo: one script (agent_slim.py) that chats + exports observability.
#  It has no web server and never runs in Teams, so there are far fewer variables
#  than a365-agent-full.
# ═══════════════════════════════════════════════════════════════════════════════
#
#  LEGEND — where each value comes from:
#    [FILL]   You provide it (from your Azure OpenAI / Foundry resource).
#    [CLI]    Generated by `a365 setup all`. Read from a365.generated.config.json.
#             The secret: run `a365 setup blueprint --show-secret` (same machine + user).
#    [SCRIPT] Written for you by a helper script — do not hand-edit.
#    [AUTO]   Sensible default already set below.
#
#  LEGEND — who reads each value (CONSUMED BY):
#    (LLM)   agent_slim.py → the Azure OpenAI client (auth is your `az login`, no key needed)
#    (OBS)   agent_slim.py → mints the observability token via the FMI 3-hop chain, then exports spans
#    (WORKIQ)agent_slim.py → the WorkIQ Mail MCP tool
# ═══════════════════════════════════════════════════════════════════════════════


# ── LLM: Azure OpenAI (Foundry) ────────────────────────────────────────────────
# Auth is your Azure CLI session — run `az login` first; NO api key is used here.
# [FILL] (LLM)  Endpoint of your Foundry resource (ends with a slash).
AZURE_OPENAI_ENDPOINT=
# [FILL] (LLM)  The model DEPLOYMENT name you created in Foundry.
AZURE_OPENAI_DEPLOYMENT=gpt-5
# [FILL] (LLM)  API version your deployment supports.
AZURE_OPENAI_API_VERSION=preview


# ── A365 identity — WHO the activities are attributed to ───────────────────────
# [CLI] (OBS)  The agentic app id ("Entra agent id") — this is what spans are attributed to.
#              NOTE: this is NOT the blueprint id. If you re-provision, update it.
AGENT365_ACTIVITY_AGENT_ID=
# [FILL] (OBS)  Friendly name shown in the admin center Activity view.
AGENT365OBSERVABILITY__AGENTNAME="agent-obs-demo"
# [CLI] (OBS)  = a365.generated.config.json → agentBlueprintId (span metadata only).
AGENT365OBSERVABILITY__AGENTBLUEPRINTID=


# ── Blueprint credentials — used to MINT the observability token (FMI chain) ────
# agent_slim.py combines these blueprint client credentials with AGENT365_ACTIVITY_AGENT_ID
# (as fmi_path) to mint a token AS the agent, then a token for the A365 Observability API.
# [CLI] (OBS)  = agentBlueprintId
CONNECTIONS__SERVICE_CONNECTION__SETTINGS__CLIENTID=
# [CLI] (OBS)  = `a365 setup blueprint --show-secret`
CONNECTIONS__SERVICE_CONNECTION__SETTINGS__CLIENTSECRET=
# [CLI] (OBS)  = your tenant id
CONNECTIONS__SERVICE_CONNECTION__SETTINGS__TENANTID=


# ── WorkIQ Mail tool ───────────────────────────────────────────────────────────
# [SCRIPT] (WORKIQ)  A short-lived Mail token. Get it with `.\refresh-mail-token.ps1`
#                    (which calls `a365 develop get-token` and writes it here). Leave empty to
#                    chat without mail tools. Expires ~1 hour — re-run the script to refresh.
BEARER_TOKEN=
